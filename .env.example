# Claude Code UI 环境配置文件
# 支持 Web 模式和 Electron 桌面应用模式的完整配置

# =============================================================================
# 服务器配置
# =============================================================================

# 后端服务器端口 (Express API + WebSocket 服务器)
# 默认会在 30000-39999 范围内自动找可用端口，避免端口冲突
# PORT=30000

# =============================================================================
# 应用程序配置
# =============================================================================

# Node.js 环境 (development 开发模式, production 生产模式)
NODE_ENV=development

# 日志级别控制 (ERROR, WARN, INFO, DEBUG)
LOG_LEVEL=INFO

# 前端调试模式 (仅在开发模式下有效，设置为 'true' 启用详细的前端调试日志)
VITE_DEBUG=false

# =============================================================================
# ELECTRON 桌面窗口配置
# =============================================================================

# 主窗口尺寸
WINDOW_WIDTH=1400
WINDOW_HEIGHT=900
WINDOW_MIN_WIDTH=800
WINDOW_MIN_HEIGHT=600

# 开发工具 (true 表示在桌面模式下始终打开开发者工具)
ENABLE_DEV_TOOLS=true

# =============================================================================
# 文件系统监听器配置
# =============================================================================

# 文件系统监听器事件防抖延迟 (毫秒)
WATCHER_DEBOUNCE_MS=300

# =============================================================================
# WEBSOCKET 配置
# =============================================================================

# WebSocket 重连延迟 (毫秒)
WS_RECONNECT_DELAY=3000

# WebSocket 心跳间隔 (毫秒)
WS_HEARTBEAT_INTERVAL=30000

# =============================================================================
# USAGE MONITOR 配置
# =============================================================================

# 缓存超时时间 (毫秒)
USAGE_CACHE_TIMEOUT=30000
USAGE_SESSION_CACHE_TIMEOUT=60000
USAGE_BLOCK_CACHE_TIMEOUT=300000

# =============================================================================
# 路径配置
# =============================================================================

# 用户主目录 (如果未设置则自动检测)
# HOME=/Users/username

# Claude 项目目录 (相对于 HOME 目录)
CLAUDE_PROJECTS_DIR=.claude/projects

# 桌面应用图标路径 (相对于 electron 目录)
DESKTOP_ICON_PATH=../public/icon.png