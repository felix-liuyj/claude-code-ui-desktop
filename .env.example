# Claude Code UI 环境配置文件
# 支持 Web 模式和 Electron 桌面应用模式的完整配置

# =============================================================================
# 服务器配置
# =============================================================================

# 后端服务器端口 (Express API + WebSocket 服务器)
PORT=3001

# =============================================================================
# 应用程序配置
# =============================================================================

# Node.js 环境 (development 开发模式, production 生产模式)
NODE_ENV=development

# =============================================================================
# ELECTRON 桌面窗口配置
# =============================================================================

# 主窗口尺寸
WINDOW_WIDTH=1400
WINDOW_HEIGHT=900
WINDOW_MIN_WIDTH=800
WINDOW_MIN_HEIGHT=600

# 开发工具 (true 表示在桌面模式下始终打开开发者工具)
ENABLE_DEV_TOOLS=true

# =============================================================================
# 文件系统监听器配置
# =============================================================================

# 文件系统监听器事件防抖延迟 (毫秒)
WATCHER_DEBOUNCE_MS=300

# =============================================================================
# WEBSOCKET 配置
# =============================================================================

# WebSocket 重连延迟 (毫秒)
WS_RECONNECT_DELAY=3000

# =============================================================================
# 路径配置
# =============================================================================

# 用户主目录 (如果未设置则自动检测)
# HOME=/Users/username

# Claude 项目目录 (相对于 HOME 目录)
CLAUDE_PROJECTS_DIR=.claude/projects

# 桌面应用图标路径 (相对于 electron 目录)
DESKTOP_ICON_PATH=../build/icon.png